import 'package:flutter/material.dart';

class AppColors {
  // Brand Colors
  static const Color primary = Color(0xFF0D9488); // Teal / Emerald 600
  static const Color primaryDark = Color(0xFF0F766E);
  static const Color primaryLight = Color(0xFF2DD4BF);
  static const Color primarySubtle = Color(0xFFCCFBF1);

  // Background & Surface
  static const Color background = Color(0xFF0B0F17); // Sleek modern dark slate
  static const Color surface = Color(0xFF151D2A);
  static const Color surfaceCard = Color(0xFF1E293B);
  static const Color surfaceElevated = Color(0xFF27354A);
  static const Color border = Color(0xFF334155);

  // Light Mode (if toggled)
  static const Color lightBackground = Color(0xFFF8FAFC);
  static const Color lightSurface = Color(0xFFFFFFFF);
  static const Color lightBorder = Color(0xFFE2E8F0);

  // Text Colors
  static const Color textPrimary = Color(0xFFF8FAFC);
  static const Color textSecondary = Color(0xFF94A3B8);
  static const Color textMuted = Color(0xFF64748B);

  // Status & Alert Colors
  static const Color success = Color(0xFF10B981); // Emerald
  static const Color successSubtle = Color(0xFF064E3B);
  static const Color warning = Color(0xFFF59E0B); // Amber (Shipper Advise / Pending)
  static const Color warningSubtle = Color(0xFF78350F);
  static const Color error = Color(0xFFEF4444); // Crimson (Failed / RTO)
  static const Color errorSubtle = Color(0xFF7F1D1D);
  static const Color info = Color(0xFF3B82F6); // Blue (Out for delivery)
  static const Color infoSubtle = Color(0xFF1E3A8A);

  // Accent Badges
  static const Color purple = Color(0xFF8B5CF6);
  static const Color purpleSubtle = Color(0xFF4C1D95);
}
